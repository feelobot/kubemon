apiVersion: v1
kind: Service
metadata:
  name: grafana
  labels:
    app: grafana
  namespace: kube-system
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
    name: external
  selector:
    app: grafana
